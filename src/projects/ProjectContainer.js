import React, { Component } from 'react';

class ProjectContainer extends Component {
    render () {
        return (
            <div>I am a project container...</div>
        );
    }
}

export default ProjectContainer;
